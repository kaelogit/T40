export const T40_PARENT_SLUG = "t40-exclusives";

/** Fallback when subcategories API is unavailable — empty so we never show ghost nav links */
export const DEFAULT_T40_SUBCATEGORY_SLUGS: readonly string[] = [];

export const T40_ACCENT = "#d94625";
